/**
 * @deprecated Use `useOnsiteCustomerData` instead.
 * This hook is the on-site/employee-mode customer loader (not a generic "secure" variant).
 * Kept as a thin re-export for backwards compatibility.
 */
export { useOnsiteCustomerData as useSecureCustomerData } from './useOnsiteCustomerData';
